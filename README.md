# Midi

This project aims to develop a script that converts MP3 audio files to MIDI.
The repository is currently being set up, and more code will follow as we experiment with audio-to-MIDI conversion.
